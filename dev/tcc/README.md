# /apps/dev/tcc placeholder

This directory is staged to `/apps/dev/tcc/` in the disk image.

It is a placeholder TinyCC runtime root (`CONFIG_TCCDIR`) and will be
populated by later toolchain slices with runtime helpers such as
`libtcc1.a`.
